select
  neighborhood_file_id,
  source_code,
  source_name,
  city,
  state,
  neighborhood,
  signal_category,
  geographic_scope,
  source_type,
  publisher,
  source_url,
  html_uri,
  json_uri,
  screenshot_uri,
  file_uri,
  metadata_uri,
  content_type,
  parse_status,
  last_error,
  reference_date,
  collected_at,
  parsed_at
from {{ ref('int_neighborhood_files_latest') }}
