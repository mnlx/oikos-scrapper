select
  id as neighborhood_file_id,
  source_code,
  source_name,
  base_url,
  source_url,
  city,
  state,
  neighborhood,
  signal_category,
  geographic_scope,
  source_type,
  publisher,
  parser,
  content_type,
  html_uri,
  json_uri,
  screenshot_uri,
  file_uri,
  metadata_uri,
  checksum_sha256,
  size_bytes,
  parse_status,
  last_error,
  reference_date,
  metadata_json,
  collected_at,
  parsed_at
from {{ source('app', 'neighborhood_files') }}
